# Copy this file to `.env` for development or `.env.production` for production.
# Then adjust values as needed. Secrets should NEVER be committed to VCS.

# --- Application ---
ENV=development
DEBUG=true
# Use scripts/generate_secret.sh to generate and set this securely
SECRET_KEY=CHANGE_ME

# --- Backend ---
BACKEND_PORT=8000
# For in-cluster communication (frontend container -> backend container)
API_BASE_URL=http://backend:8000

# --- Database ---
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio
POSTGRES_DB=portfolio
POSTGRES_PORT=5432
DATABASE_HOST=db
DATABASE_PORT=5432
# Optional: if your app supports DATABASE_URL
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${POSTGRES_DB}

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
# Optional: if your app supports REDIS_URL
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/0

# --- Networking / Observability ---
NGINX_PORT=8080
PROMETHEUS_PORT=9090

# --- CORS / Security (if supported by backend) ---
#ALLOWED_ORIGINS=*
#LOG_LEVEL=info